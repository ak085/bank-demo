/**
 * Sidebar nav entry for {{ identity.display_name_en }}.
 *
 * Merged into frontend/src/components/Sidebar.tsx in the order the framework
 * defines (see AIHVAC_DASHBOARD_FRAMEWORK.md §2.1 and §5.3). minRole is
 * always 'viewer' for equipment list pages; write actions are gated on the
 * action, not the page.
 */

// Append to NAV_CONFIG in lib/navigation-config.ts:
{
  label: "{{ identity.display_name_en }}",   // i18n key: sidebar.{{ identity.kind }}
  href: "/{{ identity.kind }}",
  icon: "{{ identity.icon }}",                // lucide-react icon name
  minRole: "viewer",
  // Generator stamps the sub-items below only if the archetype enables them.
  children: [
    // { label: "...", href: "/{{ identity.kind }}/summary", icon: "...", minRole: "viewer" },
    // { label: "...", href: "/{{ identity.kind }}/[id]",    icon: "...", minRole: "viewer" },
  ],
}
